<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sx">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--<activity android:name="com.example.sx.MainActivity"></activity>-->
        <!--注册页面-->
        <activity android:name=".FileActivity"></activity>
        <activity android:name=".FoodActivity"></activity>
        <activity android:name=".MyActivity"></activity>
        <activity android:name=".SkyActivity"></activity>
        <activity android:name=".TrianActivity"></activity>
    </application>

</manifest>